[project]
name = "skills-tracker-backend"
version = "0.1.0"
description = "REST API for tracking developer skills"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beanie>=2.0.1",
    "fastapi[standard]>=0.115.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
